{% trans_default_domain 'toolbar' %}

{% spaceless %}
{# Dashboard #}
{{ toolbar_button(path('purchasecredits_dashboard'), 'dashboard', 'md', 'label.purchasecredits'|trans) }}

{# Purchase Credits #}
{% if route_exists('user_dashboard') %}
    {{ toolbar_button(path('user_dashboard'), 'user') }}
{% endif %}

{# Purchase Credits #}
{% if type != 'transactions' %}
    {{ toolbar_button(path('purchasecredits_transactions'), 'transactions') }}
{% endif %}

{# Transactions #}
{% if type != 'purchase' %}
    {{ toolbar_button(path('purchasecredits_purchase'), 'credits') }}
{% endif %}

{# Configuration #}
{% if is_granted(config('c975LPurchaseCredits.roleNeeded')) %}
    {{ toolbar_button(path('purchasecredits_config'), 'config') }}
{% endif %}

{# Signout #}
{{ toolbar_button(path('user_signout'), 'signout') }}

{# Other dashboards #}
{{ toolbar_dashboards('md') }}
{% endspaceless %}